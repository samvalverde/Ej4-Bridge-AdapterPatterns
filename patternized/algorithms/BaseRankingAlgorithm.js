// patternized/algorithms/BaseRankingAlgorithm.js

class BaseRankingAlgorithm {
    rankPhotos(photos) {
        // Método a ser sobrescrito por las subclases
        return photos;
    }
}

module.exports = BaseRankingAlgorithm;
